package com.rauf.learn.fs

object JsonReader {
def main(args: Array[String]): Unit = {
    
    val jFilePath = """E:\logs\cslookup.txt"""
    val file = scala.io.Source.fromFile(jFilePath)
    
    
  }
}