package com.code._1_IDTypeTest

import org.scalatest.FunSuite
import com.codes._1_IDType.IDType

class IDTypeTest extends FunSuite {
  val a = List()
  test("An empty Set should have size 0") {
    assert(a.size == 0)

  }
}
